package exercises5;

public class Recipe {

	public static void main(String[] args) {

		//
		// No java code needed for this exercise
		// Below, use comments or actual Java code
		//  to type in the abstractions
		//  you found in the recipe story
		// 

		//
		// Use those abstractions in comments or code to 
		//   express the recipe
		//
		/* a = mix(water,200cc,yeast,5g);
		 * heat(better,meltpoint);
		 * b = mix(butter,vanilla);
		 * heat(a,100degree);
		 * delay 5min;
		 * c = mix(a,b,sugar,flour);
		 * heat(c,110degree);
		 * String portion = new String[10];
		 * for (int i = 0; i < 10; i++){
		 * 	portion[i] = 0.1*c;
		 * 	heat(portion[i],350ddegree);
		 * }
		 * delay 45min;
		 * 
		 */
	}

}
